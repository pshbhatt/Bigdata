In order to run the program download the DSA.jar file from github and place it at a location.
Open the terminal and write the command: export CLASSPATH=$CLASSPATH:"location of the jar file"
Navigate to hadoop/sbin and execute the DataSimilarityAnalyzer.sh script as: ./DataSimilarityAnalyzer.sh -compute <Options>/-retrieve <Options>

